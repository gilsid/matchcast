# Backend
PORT=3001
CORS_ORIGIN=http://localhost:5173
NODE_ENV=development
JWT_SECRET=change-this-to-a-random-secret

# Database (PostgreSQL)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/tournament_db?schema=public"
DATABASE_URL_UNPOOLED="postgresql://postgres:postgres@localhost:5432/tournament_db?schema=public"  # Direct (non-pooled) — for migrations in production
