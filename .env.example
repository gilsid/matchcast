# PostgreSQL connection string. URLs containing neon.tech use the Neon
# HTTP driver (Vercel); anything else uses a plain pg Pool (local dev, tests).
DATABASE_URL=postgresql://postgres@127.0.0.1:5433/matchcast_dev

# Required in production, optional locally. Boot fails without it in prod.
JWT_SECRET=change-me-in-production
